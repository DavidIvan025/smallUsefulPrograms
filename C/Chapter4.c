#include <stdio.h>

int main (void)
{
    int number, i,j;
    /* Excersice 1
    printf("Enter a two-digit number: ");
    scanf("%d", &number);
    i = number / 10;
    j = number % 10;

    printf("The reversal is: %d%d\n", j, i);
    return 0; */
}